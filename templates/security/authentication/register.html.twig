{% extends 'base.html.twig' %}

{% block body %}
<div class="page-header">
    <h1 style="text-align:center;"> Create your account </h1>
</div>
<div class="card">
    <div class="card-body">

    <ul>
        {% for error in errors %}
            <li>{{ error.message }}</li>
        {% endfor %}
    </ul> <br>
    {{ form_start(form) }}
    {{ form_end(form) }}
    </div>
</div>
{% endblock %}
